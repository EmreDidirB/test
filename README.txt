git checkout -b benim-yeni-ozelligim (Yeni branch oluştur ve geç)

Kod değişikliklerini yap

git add . (Değişiklikleri staging alanına ekle)

git commit -m "Anlamlı commit mesajı" (Değişiklikleri commit'le)

git push origin benim-yeni-ozelligim (Feature branch'i remote'a push'la)

GitHub üzerinden Pull Request Oluştur